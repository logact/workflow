// Root component of the SPA.
// Renders a minimal placeholder until real features are implemented —
// keeps the scaffold free of demo code so `vite build` output stays lean.
function App() {
  return (
    <main className="app-placeholder">
      <h1>App</h1>
      <p>Vite + React + TypeScript scaffold is running.</p>
    </main>
  )
}

export default App
